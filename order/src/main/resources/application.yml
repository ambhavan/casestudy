server:
   port: 8004
   
spring:
  application:
      name: order-micro
  data:
      mongodb:
        database=new
        host=localhost
        port=27017
eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:8761/eureka
      instance:
      preferIpAddress: true

  
